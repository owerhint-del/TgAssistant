/**
 * TgAssistant Web UI — shared utilities.
 * Page-specific logic lives in the template <script> blocks.
 */

// Nothing needed here yet — Alpine.js handles all reactivity inline.
// This file exists for future shared utilities (e.g., API client, formatters).
